@page
@model LogoutModel

@{
    ViewData["Title"] = "Log out";
}

<header>
    @{
        if (User.Identity.IsAuthenticated)
        {
            <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Page("/", new { area = "" })" method="post">
                <div class="card account-card shadow bg-body rounded">
                    <h5 class="account-card-title">
                        Kutoka
                    </h5>
                    <button type="submit" class="nav-link btn btn-link btn-lg signin-button">Bofya hapa ili Kuondoka</button>
                </div>
            </form>
        }
        else
        {
            <div class="card account-card shadow bg-body rounded">
                <h5 class="account-card-title">
                    Kutoka
                </h5>
                <h6 class="account-card-subtitle">
                    Umefanikiwa kuondoka kwenye programu.
                </h6>
            </div>
        }
    }
</header>
