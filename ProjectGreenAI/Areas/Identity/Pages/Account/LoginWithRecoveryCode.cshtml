@page
@model LoginWithRecoveryCodeModel

@{
    ViewData["Title"] = "Recovery code verification";
}

<div class="card signin-card shadow bg-white rounded pe-2 ps-2">
    <div class="card-body signin-card-body">
        <h5 class="signin-card-title text-warm fs-2 fw-bold">
            Ingia
        </h5>
        <h6><span class="badge bg-warm mt-2">Karibu mzazi.</span></h6>
        <p>
            Umeomba kuingia kwa kutumia msimbo wa kurejesha akaunti. Kuingia huku hakutakumbukwa hadi utoe
            msimbo wa programu ya uthibitishaji katika ingia au zima 2FA na uingie tena.
        </p>

        <div>
            <section>
                <form method="post">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="form-group">
                        <div class="signin-textbox-group">
                            <input asp-for="Input.RecoveryCode" class="form-control signin-textboxes" autocomplete="off" placeholder="Kanuni yangu ni ..." />
                            <span asp-validation-for="Input.RecoveryCode" class="text-danger signin-validation-text"></span>
                        </div>

                        <div>
                            <button type="submit" class="btn btn-warm btn-lg signin-button">Ingia</button>
                        </div>
                    </div>
                </form>
            </section>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}