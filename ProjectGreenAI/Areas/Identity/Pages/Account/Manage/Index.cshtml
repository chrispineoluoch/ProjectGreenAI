@page
@model IndexModel
@{
    ViewData["Title"] = "Profile";
    ViewData["ActivePage"] = ManageNavPages.Index;
}

<partial name="_StatusMessage" for="StatusMessage" />
<div class="card Page-input-card mb-4 bg-white shadow rounded pe-2 ps-2">
    <div class="card-body">
        <h5 class="signin-card-title text-warm fs-2">
            Dhibiti Akaunti
        </h5>
        <h6><span class="badge bg-warm mt-2">Karibu mzazi.</span></h6>

        <div>
            <section>
                <form id="profile-form" method="post">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="form-group">
                        <div class="signin-textbox-group">
                            <input asp-for="Username" class="form-control signin-textboxes" autocomplete="username" placeholder="Barua pepe yangu ni ..." aria-required="true" />
                            <span asp-validation-for="Username" class="text-danger signin-validation-text"></span>

                            <input asp-for="Input.PhoneNumber" class="form-control signin-textboxes" autocomplete="current-password" placeholder="Nambari yangu ya simu ni ..." aria-required="true" />
                            <span asp-validation-for="Input.PhoneNumber" class="text-danger signin-validation-text"></span>
                        </div>

                        <div>
                            <button id="update-profile-button" type="submit" class="btn btn-warm btn-lg rounded-pill Page-button-transform">Hifadhi</button>
                        </div>
                    </div>
                </form>
            </section>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
