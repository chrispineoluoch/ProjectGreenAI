@page
@model LoginWith2faModel

@{
    ViewData["Title"] = "Two-factor authentication";
}

<div class="card signin-card shadow bg-white rounded pe-2 ps-2">
    <div class="card-body signin-card-body">
        <h5 class="signin-card-title text-warm fs-2 fw-bold">
            Ingia
        </h5>
        <h6><span class="badge bg-warm mt-2">Karibu mzazi.</span></h6>

        <div>
            <section>
                <form method="post" asp-route-returnUrl="@Model.ReturnUrl">
                    <input asp-for="RememberMe" type="hidden" />
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="form-group">
                        <div class="signin-textbox-group">
                            <input asp-for="Input.TwoFactorCode" class="form-control signin-textboxes" autocomplete="off" placeholder="Kanuni yangu ni ..." />
                            <span asp-validation-for="Input.TwoFactorCode" class="text-danger signin-validation-text"></span>
                        </div>

                        <div>
                            <button type="submit" class="btn btn-warm btn-lg signin-button">Ingia</button>
                        </div>

                        <div class="or-separator mt-lg-3 snapple-seperator">
                            <span class="or-text">au</span>
                        </div>
                    </div>

                    <div class="join-now-div">
                        <p>Je, huna idhini ya kufikia kifaa chako cha uthibitishaji? Unaweza &nbsp</p><a asp-page="./LoginWithRecoveryCode" asp-route-returnUrl="@Model.ReturnUrl" class="join-now-link">ingia na msimbo wa kurejesha</a>
                    </div>
                </form>
            </section>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}